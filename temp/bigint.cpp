#include<bits/stdc++.h>
using namespace std;

int main()
{
    vector<priority_queue<pair<int, int> > >vq;     //radius, index
    while(scanf("%d %d %d", &n, &l, &w) != EOF) {
        for(register int i = 0; i < n; i++)
            scanf("%d", &x);
            r =
    }
    return 0;
}
