#include <bits/stdc++.h>
using namespace std;
typedef long long ll;


int main() {
    int t;
    scanf("%d", &t);
    
    
    for(int Case = 1; Case <= t; ++Case) {
        scanf("%d%d%d%d", &n, &m, &a, &b);
        
        v.clear();
        for(int i = 0; i < m; ++i)
            v.push_back(0);
        for(int i = 0; i < n; ++i)
            v.push_back(1);
            
        ll sum = 0;
        do {
            sum = 0;
            for(auto it : v)
                sum = sum*10 + it;
