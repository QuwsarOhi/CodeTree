#include<bits/stdc++.h>
using namespace std;

bool cmp(pair<int, int>, pair<int, int>)
{

}

int main()
{
    int n;
    while(scanf("%d", &n) && n) {
        vector<pair<int, int> >v;
        for(int i = 0; i < n; i++) {
            scanf("%d", &x);
            v.push_back(make_pair(x, i));
        }

    }
}
