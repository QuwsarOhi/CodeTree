#include <bits/stdc++.h>
using namespace std;


int main() {
    string str;
    while(getline(cin, str))
        v.push_back(str);

    for(auto it : v) {
        stringstream ss;
        ss << it;

        string add;
        while(it >> tok) {
            if(tok[0] == "/") {
                
            }
        }
    }
}